# Secure Pass & Vault (Генератор паролей)

Веб-приложение для генерации сложных паролей с гибкими настройками и облачным криптографическим хранилищем. Проект разрабатывается в рамках кейса «Разработка приложения для генерации сложных паролей»[cite: 2].

## О проекте
[cite_start]Приложение решает прикладную проблему создания устойчивых паролей под разные требования. В отличие от простых генераторов, наша система предлагает SaaS-подход: пользователи могут регистрироваться, сохранять любимые настройки (пресеты) [cite: 31] и безопасно хранить историю генераций. 
Все сохраненные пароли шифруются на бэкенде с использованием уникального «мастер-ключа» пользователя, который нигде не сохраняется.

## Архитектура и Стек технологий
Проект построен по микросервисной (serverless) архитектуре в виде монорепозитория:
* [cite_start]**Frontend:** React (SPA) — папка `/frontend`
* [cite_start]**Backend:** Python / Flask (Serverless функции на Yandex Cloud API Gateway) — папка `/backend`
* [cite_start]**База Данных:** PostgreSQL (Managed Service) — папка `/database`

### Структура репозитория
```text
├── frontend/       # Клиентская часть (React, UI компоненты, API-сервисы)
├── backend/        # Серверная часть (Flask, бизнес-логика, шифрование)
├── database/       # SQL-скрипты, миграции и схема БД
├── docs/           # C4-диаграммы, API-контракты и документация
└── README.md       # Описание проекта